using System.Threading.Tasks;

namespace System.EventSourcing.Hosting.Middleware
{
    public static class UseExtensions
    {
        // public static IMiddlewareAppBuilder<TContext> Use<TContext>(this IMiddlewareAppBuilder<TContext> app, Func<TContext, Func<Task>, Task> middleware)
        //     where TContext : IContext
        // {
        //     return app.Use()
        // }
    }
}
