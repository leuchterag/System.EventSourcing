using System.EventSourcing.Hosting.Middleware;
using System.Threading.Tasks;

namespace System.EventSourcing.Hosting.Transformation
{
    public static class FilterExpressionExtensions<TKey, TContent>
    {
        public static EventMiddleware<TKey, TContent> AllOf(params EventMiddleware<TKey, TContent>[] middlewares)
        {
            return (ctx) =>
            {
                return Task.CompletedTask;
            };
        }
    }
}
